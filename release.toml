# cargo-release configuration
# https://github.com/crate-ci/cargo-release

# GitHub Actions publishes after the release tag passes CI.
publish = false
push = true
tag = true

tag-name = "v{{version}}"
